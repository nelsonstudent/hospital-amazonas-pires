apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: staging

commonLabels:
  environment: staging

patches:
  - path: patches/hpa.yaml
  - path: patches/ingress.yaml

images:
  - name: nelsonstudent/hospital-amazonas-pires
    newName: nelsonstudent/hospital-amazonas-pires
    newTag: latest
